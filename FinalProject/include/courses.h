#ifndef COURSES_H
#define COURSES_H

#include <string>
#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
class courses
{
    public:
        courses(std::string csvFileName);
        virtual ~courses();
    protected:
    private:

};

#endif // COURSES_H
